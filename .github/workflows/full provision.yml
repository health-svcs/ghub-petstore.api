name: Provision EC2 + Build & Deploy API

on:
  workflow_dispatch:
    inputs:
      APP_NAME:
        description: "App label for DNS/ECR (petstore.api, verademo.api, etc)"
        required: false
        default: "petstore.api"
        type: string
      SA_ACRONYM:
        description: "3-letter SA acronym for stable DNS (agc, elg, etc)"
        required: true
        default: "agc"
        type: string
      TlsMode:
        description: "TLS posture applied at deploy time (NGINX): Weak or Secure"
        required: true
        default: "Weak"
        type: choice
        options:
          - Weak
          - Secure

  push:
    branches: ["main"]

env:
  AWS_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
  AWS_REGION: ${{ vars.AWS_REGION }}

  APP_NAME: ${{ inputs.APP_NAME || vars.APP_NAME_DEFAULT }}
  SA_ACRONYM: ${{ inputs.SA_ACRONYM || vars.SA_ACRONYM_DEFAULT }}
  TLS_MODE: ${{ inputs.TlsMode || vars.TLSMODE_DEFAULT || 'Weak' }}

  ECR_REPO_RAW: ${{ inputs.APP_NAME || vars.APP_NAME_DEFAULT }}
  IMAGE_TAG: ${{ github.run_id }}

  TEMPLATE_FILE: cloudformation/ec2.yaml
  HOSTED_ZONE_ID: ${{ vars.HOSTED_ZONE_ID_SA }}
  DNS_BASE_DOMAIN: ${{ vars.SA_BASE_DOMAIN }}
  DNS_TTL: ${{ vars.DNS_TTL || '60' }}

  DEMO_OWNER: ${{ vars.DEMO_OWNER || github.actor }}

jobs:
  validate-inputs:
    runs-on: ubuntu-latest
    steps:
      - name: Validate required variables
        shell: bash
        run: |
          set -euo pipefail
          require() { local n="$1"; local v="$2"; [[ -n "${v}" ]] || { echo "ERROR: '${n}' is empty"; exit 1; }; }

          require "AWS_ACCOUNT_ID" "${AWS_ACCOUNT_ID}"
          require "AWS_REGION" "${AWS_REGION}"
          require "AWS_ROLE_TO_ASSUME" "${{ vars.AWS_ROLE_TO_ASSUME }}"

          require "AMI_ID" "${{ vars.AMI_ID }}"
          require "INSTANCE_TYPE" "${{ vars.INSTANCE_TYPE }}"
          require "SUBNET_ID" "${{ vars.SUBNET_ID }}"
          require "SG_IDS" "${{ vars.SG_IDS }}"
          require "INSTANCE_PROFILE_NAME" "${{ vars.INSTANCE_PROFILE_NAME }}"
          require "KEY_NAME" "${{ vars.KEY_NAME }}"

          require "APP_NAME_DEFAULT" "${{ vars.APP_NAME_DEFAULT }}"
          require "SA_ACRONYM_DEFAULT" "${{ vars.SA_ACRONYM_DEFAULT }}"
          require "HOSTED_ZONE_ID_SA" "${{ vars.HOSTED_ZONE_ID_SA }}"
          require "SA_BASE_DOMAIN" "${{ vars.SA_BASE_DOMAIN }}"

          if ! [[ "${DNS_TTL}" =~ ^[0-9]+$ ]]; then
            echo "ERROR: DNS_TTL must be numeric, got: '${DNS_TTL}'"
            exit 1
          fi

          if [[ "${DEMO_OWNER}" == "github-actions" || "${DEMO_OWNER}" == *"[bot]" ]]; then
            echo "ERROR: Refusing to provision for automation actor: ${DEMO_OWNER}"
            exit 1
          fi

  provision:
    runs-on: ubuntu-latest
    needs: validate-inputs
    permissions:
      id-token: write
      contents: read
    outputs:
      instance_id: ${{ steps.out.outputs.instance_id }}
      public_ip: ${{ steps.out.outputs.public_ip }}
      dns_name: ${{ steps.out.outputs.dns_name }}
      dast_url: ${{ steps.out.outputs.dast_url }}
      ecr_repo: ${{ steps.out.outputs.ecr_repo }}

    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Derive names + deploy CFN + wait + DNS
        id: out
        shell: bash
        run: |
          set -euo pipefail

          norm() { printf "%s" "$1" | tr -d '\r\n' | xargs; }

          APP="$(norm "${APP_NAME}")"
          ACR="$(norm "${SA_ACRONYM}")"
          BASE="$(norm "${DNS_BASE_DOMAIN}")"

          SAFE_STACK_APP="$(echo "${APP}" | tr '._' '-' | tr -cd 'a-zA-Z0-9-')"
          SAFE_ACTOR="$(echo "${GITHUB_ACTOR}" | tr -cd 'a-zA-Z0-9-')"
          STACK_NAME="${SAFE_STACK_APP}-ec2-${SAFE_ACTOR}-${GITHUB_RUN_ID}"

          SAFE_ECR="$(echo "${ECR_REPO_RAW}" | tr '[:upper:]' '[:lower:]' | tr '._' '-' | tr -cd 'a-z0-9/-')"
          [[ -n "${SAFE_ECR}" ]] || SAFE_ECR="petstore-api"

          echo "Deploying stack: ${STACK_NAME}"
          aws cloudformation deploy \
            --stack-name "${STACK_NAME}" \
            --template-file "${TEMPLATE_FILE}" \
            --parameter-overrides \
              "AMIId=${{ vars.AMI_ID }}" \
              "InstanceType=${{ vars.INSTANCE_TYPE }}" \
              "SubnetId=${{ vars.SUBNET_ID }}" \
              "SecurityGroupIds=${{ vars.SG_IDS }}" \
              "IamInstanceProfileName=${{ vars.INSTANCE_PROFILE_NAME }}" \
              "KeyName=${{ vars.KEY_NAME }}" \
              "DeployTagKey=DemoOwner" \
              "DeployTagValue=${DEMO_OWNER}" \
              "AppName=${APP_NAME}" \
              "TlsMode=${TLS_MODE}" \
              "ImageTag=${IMAGE_TAG}" \
            --capabilities CAPABILITY_NAMED_IAM \
            --no-fail-on-empty-changeset

          INSTANCE_ID="$(aws cloudformation describe-stack-resources \
            --stack-name "${STACK_NAME}" \
            --query "StackResources[?LogicalResourceId=='ApiInstance'].PhysicalResourceId" \
            --output text)"
          [[ -n "${INSTANCE_ID}" && "${INSTANCE_ID}" != "None" ]] || { echo "ERROR: Could not resolve ApiInstance"; exit 1; }
          echo "InstanceId: ${INSTANCE_ID}"

          aws ec2 wait instance-running --instance-ids "${INSTANCE_ID}"
          aws ec2 wait instance-status-ok --instance-ids "${INSTANCE_ID}"

          # Wait for SSM Online
          for i in {1..90}; do
            STATUS="$(aws ssm describe-instance-information \
              --filters Key=InstanceIds,Values="${INSTANCE_ID}" \
              --query "InstanceInformationList[0].PingStatus" \
              --output text || true)"
            echo "SSM: ${STATUS}"
            [[ "${STATUS}" == "Online" ]] && break
            sleep 10
          done
          [[ "${STATUS}" == "Online" ]] || { echo "ERROR: SSM not Online"; exit 1; }

          PUBLIC_IP="$(aws ec2 describe-instances \
            --instance-ids "${INSTANCE_ID}" \
            --query "Reservations[0].Instances[0].PublicIpAddress" \
            --output text)"
          [[ -n "${PUBLIC_IP}" && "${PUBLIC_IP}" != "None" ]] || { echo "ERROR: No public IP"; exit 1; }
          echo "PublicIP: ${PUBLIC_IP}"

          # DNS-safe pieces
          APP_DNS="$(echo "${APP}" | tr '[:upper:]' '[:lower:]' | tr '_' '-' | tr -cd 'a-z0-9.-' | sed 's/^\.*//;s/\.*$//')"
          ACR_DNS="$(echo "${ACR}" | tr '[:upper:]' '[:lower:]' | tr '_' '-' | tr -cd 'a-z0-9-'  | sed 's/^-*//;s/-*$//')"
          BASE_DNS="$(echo "${BASE}" | tr '[:upper:]' '[:lower:]' | tr '_' '-' | tr -cd 'a-z0-9.-' | sed 's/^\.*//;s/\.*$//')"

          DNS_NAME="${APP_DNS}.${ACR_DNS}.${BASE_DNS}"
          DAST_URL="https://${DNS_NAME}"

          jq -n \
            --arg name "${DNS_NAME}." \
            --arg ip "${PUBLIC_IP}" \
            --argjson ttl "${DNS_TTL}" \
            '{Changes:[{Action:"UPSERT",ResourceRecordSet:{Name:$name,Type:"A",TTL:$ttl,ResourceRecords:[{Value:$ip}]}}]}' \
          > change.json

          aws route53 change-resource-record-sets \
            --hosted-zone-id "${HOSTED_ZONE_ID}" \
            --change-batch file://change.json

          echo "instance_id=${INSTANCE_ID}" >> "$GITHUB_OUTPUT"
          echo "public_ip=${PUBLIC_IP}" >> "$GITHUB_OUTPUT"
          echo "dns_name=${DNS_NAME}" >> "$GITHUB_OUTPUT"
          echo "dast_url=${DAST_URL}" >> "$GITHUB_OUTPUT"
          echo "ecr_repo=${SAFE_ECR}" >> "$GITHUB_OUTPUT"

  build-and-push:
    runs-on: ubuntu-latest
    needs: provision
    permissions:
      id-token: write
      contents: read
    outputs:
      image_uri: ${{ steps.img.outputs.image_uri }}

    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Ensure ECR repo exists
        shell: bash
        run: |
          set -euo pipefail
          ECR="${{ needs.provision.outputs.ecr_repo }}"
          aws ecr describe-repositories --repository-names "${ECR}" >/dev/null 2>&1 || \
            aws ecr create-repository --repository-name "${ECR}" >/dev/null

      - name: Build & Push
        id: img
        shell: bash
        run: |
          set -euo pipefail
          ECR="${{ needs.provision.outputs.ecr_repo }}"
          REGISTRY="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
          IMAGE_URI="${REGISTRY}/${ECR}:${IMAGE_TAG}"

          aws ecr get-login-password --region "${AWS_REGION}" | \
            docker login --username AWS --password-stdin "${REGISTRY}"

          docker build -t "${IMAGE_URI}" .
          docker push "${IMAGE_URI}"

          echo "image_uri=${IMAGE_URI}" >> "$GITHUB_OUTPUT"

  deploy:
    runs-on: ubuntu-latest
    needs: [provision, build-and-push]
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Configure nginx + run container via SSM (and wait for completion)
        shell: bash
        run: |
          set -euo pipefail

          INSTANCE_ID="${{ needs.provision.outputs.instance_id }}"
          IMAGE_URI="${{ needs.build-and-push.outputs.image_uri }}"
          REGISTRY="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
          TLS_MODE="${TLS_MODE}"

          # Build one bash script payload (no nested heredocs in YAML)
          REMOTE_SCRIPT="$(cat <<'BASH'
bash -lc '
set -euo pipefail

sudo apt-get update -y
sudo DEBIAN_FRONTEND=noninteractive apt-get install -y docker.io nginx openssl ca-certificates awscli curl jq

sudo systemctl enable --now docker
sudo systemctl enable --now nginx

sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/nginx/self.key -out /etc/nginx/self.crt \
  -subj "/CN=localhost"

sudo mkdir -p /etc/nginx/conf.d

if [ "TLS_MODE_VALUE" = "Weak" ]; then
  sudo tee /etc/nginx/conf.d/app.conf >/dev/null <<EOF
server {
  listen 443 ssl;
  ssl_certificate     /etc/nginx/self.crt;
  ssl_certificate_key /etc/nginx/self.key;
  ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
  ssl_ciphers HIGH:!aNULL:!MD5;
  ssl_prefer_server_ciphers on;

  location = /tls-info { default_type text/plain; return 200 "mode=Weak"; }
  location / { proxy_pass http://127.0.0.1:5000; }
}
EOF
else
  sudo tee /etc/nginx/conf.d/app.conf >/dev/null <<EOF
server {
  listen 443 ssl;
  ssl_certificate     /etc/nginx/self.crt;
  ssl_certificate_key /etc/nginx/self.key;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_ciphers ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
  ssl_prefer_server_ciphers on;
  add_header Strict-Transport-Security "max-age=63072000" always;

  location = /tls-info { default_type text/plain; return 200 "mode=Secure"; }
  location / { proxy_pass http://127.0.0.1:5000; }
}
EOF
fi

sudo rm -f /etc/nginx/sites-enabled/default || true
sudo nginx -t
sudo systemctl restart nginx

aws ecr get-login-password --region AWS_REGION_VALUE | \
  docker login --username AWS --password-stdin REGISTRY_VALUE

docker pull IMAGE_URI_VALUE
docker rm -f petstore || true
docker run -d --name petstore --restart unless-stopped -p 127.0.0.1:5000:5000 IMAGE_URI_VALUE || true

echo "Local check:"
curl -sS -o /dev/null -w "http://127.0.0.1:5000/ => HTTP %{http_code}\n" http://127.0.0.1:5000/ || true

sudo ss -lntp | egrep ":(443|5000)\b" || true
sudo tail -n 60 /var/log/nginx/error.log || true
docker logs --tail 120 petstore || true
'
BASH
)"

          # Replace placeholders (simple, safe)
          REMOTE_SCRIPT="${REMOTE_SCRIPT//TLS_MODE_VALUE/${TLS_MODE}}"
          REMOTE_SCRIPT="${REMOTE_SCRIPT//AWS_REGION_VALUE/${AWS_REGION}}"
          REMOTE_SCRIPT="${REMOTE_SCRIPT//REGISTRY_VALUE/${REGISTRY}}"
          REMOTE_SCRIPT="${REMOTE_SCRIPT//IMAGE_URI_VALUE/${IMAGE_URI}}"

          PARAMS="$(jq -nc --arg cmd "${REMOTE_SCRIPT}" '{commands:[$cmd]}')"

          COMMAND_ID="$(
            aws ssm send-command \
              --instance-ids "${INSTANCE_ID}" \
              --document-name "AWS-RunShellScript" \
              --parameters "${PARAMS}" \
              --query "Command.CommandId" \
              --output text
          )"

          echo "SSM CommandId: ${COMMAND_ID}"

          for i in {1..120}; do
            STATUS="$(aws ssm get-command-invocation \
              --command-id "${COMMAND_ID}" \
              --instance-id "${INSTANCE_ID}" \
              --query "Status" \
              --output text || true)"
            echo "Status: ${STATUS}"
            case "${STATUS}" in
              Success|Cancelled|TimedOut|Failed) break ;;
            esac
            sleep 5
          done

          echo "=== SSM stdout ==="
          aws ssm get-command-invocation \
            --command-id "${COMMAND_ID}" \
            --instance-id "${INSTANCE_ID}" \
            --query "StandardOutputContent" \
            --output text || true

          echo "=== SSM stderr ==="
          aws ssm get-command-invocation \
            --command-id "${COMMAND_ID}" \
            --instance-id "${INSTANCE_ID}" \
            --query "StandardErrorContent" \
            --output text || true

      - name: Print endpoint
        shell: bash
        run: |
          echo "DAST URL: ${{ needs.provision.outputs.dast_url }}"
          echo "Test:     ${{ needs.provision.outputs.dast_url }}/tls-info"
